# AI Advisor (Groq). Set this in Vercel → Project → Settings → Environment Variables.
# Locally, the dev/preview server also falls back to ../jwd-web/.env.local if present.
GROQ_API_KEY=your_groq_api_key_here
